#include<stdio.h>
int main()
{
    int marks[5]={40,50,60,71,83};
    int oddsum=0;
    int evensum=0;
    for(int i=0;i<5;i++)
    {
        if(marks[i]%2==0)
        {
            evensum+=marks[i];
        }
        else{
            oddsum+=marks[i];
            
        }
    }
printf("%d\n",oddsum);
    printf("%d",evensum);
}
