#include<stdio.h>
int main()
{
    int marks[5]={40,50,83,40,20};
    int max=marks[0];
    for(int i=0;i<5;i++)
    {
        if(marks[i]>max)
        {
            max=marks[i];
        }
        
    printf("%d",max);
    }
